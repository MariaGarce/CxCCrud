@page
@model CRUDCxC.Pages.DocumentsType.DetailsModel

@{
    Layout = "_Layout";
}

<!DOCTYPE html>

<html>

<head>
    <meta name="viewport" content="width=device-width" />
    <title>Details</title>
</head>

<body>

    <div>
        <h4>DocumentType</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.DocumentType.Description)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.DocumentType.Description)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.DocumentType.AccountingAccount)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.DocumentType.AccountingAccount)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.DocumentType.Status)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.DocumentType.Status)
            </dd>
        </dl>
    </div>
    <div>
        <a asp-page="./Edit" asp-route-id="@Model.DocumentType.Id">Edit</a> |
        <a asp-page="./Index">Back to List</a>
    </div>
</body>

</html>
